packages:
  # Install dbt_utils from hub
  - package: dbt-labs/dbt_utils
    version: [">=1.3.0", "<1.4.0"]  # tested with 1.3.3

  # Install dbt_date from hub (needed for dbt_date macros)
  - package: godatadriven/dbt_date
    version: [">=0.17.2", "<0.18.0"]  # tested with 0.17.2

  # Install local package (one directory above)
  - local: ../
  # Install dbt_date upstream integration project for helper macros and test definitions
  - local: dbt_packages/dbt_date/integration_tests
